name: app_todolist_supabase
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.12.4
  provider: ^6.1.5
  flutter_dotenv: ^6.0.0
  pdf: ^3.11.3
  printing: ^5.14.2
  open_file: ^3.5.10
  share_plus: ^12.0.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
